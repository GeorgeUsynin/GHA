name: "Test workflow"
on: [push, pull_request, workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: "Install deps"
        run: npm install
      - name: "Run tests"
        run: npm run test