name: "Test workflow"
on: 
  push:
    branches:
      - bbbranch
  pull_request:
    types: [opened, synchronize, edited]   

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        uses: "./.github/actions/install_deps_from_cache"
      - name: "Run tests"
        run: npm run test